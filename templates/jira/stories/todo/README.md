# todo

Story files not started — backlog for active epic(s). Added by **`/aaa-pm-plan`** or **`/aaa-pm-task-add`** (new story).

Moves to `inprogress/` when Dev starts the story (e.g. **`/aaa-pm-task-start`** on first task) or Jira status changes via **`/aaa-pm-sync`**.

Format: `templates/references/jira-story-file.md`
