# inprogress

Stories being implemented. Task files for this story are usually in `jira/tasks/todo/` or `inprogress/`.

Moves to `done/` when all acceptance criteria pass and story is ship-ready (QA signs off).

Format: `templates/references/jira-story-file.md`
