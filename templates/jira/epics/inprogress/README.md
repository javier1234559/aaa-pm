# inprogress

Active epic(s) — work in flight. **Status:** `IN_PROGRESS` on OVERVIEW; listed in `active_epics` frontmatter.

Stories for these epics live under `jira/stories/`. Moves to `done/` when epic ships.

Format: `templates/references/jira-epic-file.md`
